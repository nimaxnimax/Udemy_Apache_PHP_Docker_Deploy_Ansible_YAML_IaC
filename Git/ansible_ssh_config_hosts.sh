ssh-keygen

# example >> ssh-copy-id >> copy and install key >> server >> ubuntu1 & 2 >> username >> adrian
ssh-copy-id adrian@ubuntu1
ssh-copy-id adrian@ubuntu2

# to test and verify >> ssh using username >> example: adrian username
ssh adrian@ubuntu1
ssh adrian@ubuntu2

